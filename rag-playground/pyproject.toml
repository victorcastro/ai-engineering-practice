[project]
name = "rag-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Victor Castro", email = "victorcastro.io@icloud.com" }
]
requires-python = ">=3.11"
dependencies = [
    "anthropic>=1.5.0",
    "chromadb>=1.5.9",
    "python-dotenv>=1.2.3",
    "voyageai>=0.5.0",
]

[project.scripts]
rag-playground = "rag_playground:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
